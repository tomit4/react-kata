import { heading } from "copy/landing";
import "./Landing.css";

const Landing = () => {
  return (
    <div className="splash">
      <h1>{heading}</h1>
      {/* TODO: Landing logo */}
    </div>
  );
};

export default Landing;
